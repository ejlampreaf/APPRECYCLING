<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.splash">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Splash">
        <activity android:name=".Info_CuidadosAmbientales">

        </activity>
        <activity
            android:name=".Info_ElementosReciclablesActivity"
            android:theme="@style/Theme.Splash_2">

        </activity>
        <activity
            android:name=".activity_benitem7"
            android:theme="@style/Theme.Splash_2" />
        <activity
            android:name=".activity_benitem6"
            android:theme="@style/Theme.Splash_2" />
        <activity
            android:name=".activity_benitem5"
            android:theme="@style/Theme.Splash_2" />
        <activity
            android:name=".activity_benitem4"
            android:theme="@style/Theme.Splash_2" />
        <activity
            android:name=".activity_benitem3"
            android:theme="@style/Theme.Splash_2" />
        <activity
            android:name=".activity_benitem2"
            android:theme="@style/Theme.Splash_2" />
        <activity
            android:name=".activity_benitem1"
            android:theme="@style/Theme.Splash_2" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ui.MapsActivityLugares"
            android:label="@string/title_activity_maps_lugares" />
        <activity android:name=".activity_lugares" />
        <activity android:name=".MainActivity2" />
        <activity
            android:name=".InfoActivity"
            android:theme="@style/Theme.Splash_2" />
        <activity
            android:name=".BeneficiosActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".registroActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".Info_TiposActivity"></activity>
        <activity
            android:name=".Info_TiposDescripcionActivity"
            android:theme="@style/Theme.Splash_2" />
        <activity android:name=".Loginactivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar.Fullscreen" />
        <activity
            android:name=".MenuApp"
            android:theme="@style/Theme.Splash.NoActionBar" />
    </application>

</manifest>